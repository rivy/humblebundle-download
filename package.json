{
	"name": "humblebundle-download",
	"version": "2.0.0",
	"license": "Unlicense",
	"main": "index.js",
	"bin": {
		"humblebundle-download": "index.js"
	},
	"scripts": {
		"default # run package": "",
		"default": "node index.js",
		"# fix # fix package issues (automated/non-interactive)": "",
		"fix": "run-s fix:*",
		"# fix:lint # fix `ESLint` issues": "",
		"fix:lint": "eslint . --fix",
		"# fix:style # fix `Prettier` formatting issues": "",
		"fix:style": "prettier . --write",
		"# help # display help": "",
		"help": "< package.json node -e \"s = {p:'',e:'npm'}; if (new String(process.env.npm_execpath).match(/yarn.js$/)) { s = {p:'\\n',e:'yarn'}; }; console.log('%susage: \\`%s run TARGET [TARGET..]\\`\\n\\nTARGETs:\\n', s.p, s.e); re = /^.*?\\x22(?:\\W+\\s+)([^#\\x22]+)\\s+#+\\s+([^#\\x22]+?)(\\s+#+)?\\x22.*$/; require('readline').createInterface({ input: process.stdin, output: process.stdout, terminal: false }).on('line', function(line){ if (match = re.exec(line)) { console.log('%s %s', match[1].padEnd(19), match[2]); } });\"",
		"install-local # install package as a CLI command to local machine (as a global command)": "",
		"install-local": "npm install -g .",
		"# lint # check for package code 'lint'": "",
		"lint": "run-s --silent _:min-node-8 && run-p lint:* || echo-cli \"[lint] WARN Lint checks skipped [for NodeJS < v8]\"",
		"# lint:lint # check for code 'lint' (using `ESLint`)": "",
		"lint:lint": "eslint .",
		"# lint:spell # check for spelling errors (using `cSpell`)": "",
		"lint:spell": "cspell {eg,examples,src,test}/**/* CHANGELOG{,.*} LICENSE{,.*} README{,.*} --no-summary",
		"# lint:style # check for format imperfections (using `Prettier`)": "",
		"lint:style": "prettier . --list-different",
		"# test # test package": "",
		"test": "run-s lint",
		"_:min-node-8": "is-node-modern 8"
	},
	"repository": {
		"type": "git",
		"url": "https://github.com/rivy/humblebundle-download.git"
	},
	"author": "David Marby <david@dmarby.se> http://dmarby.se",
	"contributors": [
		"Roy Ivy III <rivy.dev@gmail.com> (http://github.com/rivy)",
		"J. Longman (https://github.com/jlongman)",
		"Johannes Löthberg (https://github.com/kyrias)"
	],
	"keywords": [
		"cli",
		"download",
		"downloader",
		"humblebundle"
	],
	"dependencies": {
		"async": "^2.6.3",
		"bottleneck": "^2.19.5",
		"breeze": "^1.2.2",
		"cli-progress": "^2.1.1",
		"colors": "^1.4.0",
		"commander": "^2.20.3",
		"enquirer": "^2.3.6",
		"fs-extra": "^8.1.0",
		"locate-path": "^5.0.0",
		"make-dir": "^3.0.2",
		"nasa-keypath": "0.0.1",
		"nightmare": "^3.0.2",
		"readline": "^1.3.0",
		"request": "^2.88.2",
		"sanitize-filename": "^1.6.3",
		"xdg-app-paths": "^5.3.0"
	},
	"devDependencies": {
		"cspell": "^3.2.14",
		"eslint": "^7.11.0",
		"eslint-config-prettier": "^6.11.0",
		"eslint-plugin-eslint-comments": "^3.2.0",
		"eslint-plugin-import": "^2.22.0",
		"is-node-modern": "^1.0.0",
		"npm-run-all": "^4.1.5",
		"prettier": "^2.1.2",
		"shelljs": "^0.8.3",
		"shx": "^0.3.2"
	},
	"prettier # overrides/[*.markdown]/tabWidth": "// set to 4 when https://github.com/prettier/prettier/issues/5019 is fixed",
	"prettier": {
		"printWidth": 100,
		"singleQuote": true,
		"useTabs": true,
		"tabWidth": 2,
		"overrides": [
			{
				"files": [
					"*.markdown",
					"*.md",
					"*.mkd"
				],
				"options": {
					"tabWidth": 2,
					"useTabs": false
				}
			}
		]
	}
}
